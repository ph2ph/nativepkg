# This file was autogenerated by nativepkg {{ generator_version }}

description "{{ package_description_shell }}"
author "{{ package_maintainer_shell }}"

start on runlevel [2345]
stop on runlevel [016]
respawn

# Privileges are dropped by the job itself, not by invoking sudo.
setuid {{ user }}
setgid {{ group }}

chdir {{ install_dir }}/{{ package_name }}

script
  . "/etc/default/{{ package_name }}"
  exec app/{{ daemon_entrypoint }}
end script
