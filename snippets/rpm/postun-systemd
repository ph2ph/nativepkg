# `%systemd_postun_with_restart`: reload, then restart only on an upgrade (package still installed).
systemctl daemon-reload >/dev/null 2>&1 || :
if [ "$1" -ge 1 ]; then
	systemctl try-restart '{{ package_name }}.service' >/dev/null 2>&1 || :
fi
