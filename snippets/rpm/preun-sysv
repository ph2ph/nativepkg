if [ "$1" -eq 0 ]; then
	if command -v service >/dev/null 2>&1; then
		service '{{ package_name }}' stop >/dev/null 2>&1 || :
	fi
	if command -v chkconfig >/dev/null 2>&1; then
		chkconfig --del '{{ package_name }}' >/dev/null 2>&1 || :
	fi
fi
